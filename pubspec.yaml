name: energeist
description: Energy consumption estimation and tracking system for household appliances using Energy Star data
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
